<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="officea.leveransapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="officea.leveransapp.CountryActivity"
            android:exported="true"
            //android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="officea.leveransapp.CityActivity"
            android:exported="true">
        </activity>

        <activity
            android:name="officea.leveransapp.RestaurantActivity"
            android:exported="true">
        </activity>

        <activity
            android:name="officea.leveransapp.OrderActivity"
            android:exported="true">
        </activity>

        <activity
            android:name="officea.leveransapp.DeliveryActivity"
            android:exported="true">
        </activity>

    </application>

</manifest>